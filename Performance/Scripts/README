1) run the `run_stat_cpu.sh` or `run_stat_gpu.sh` to produce the `Results_timing*.csv` files

2) mv those files to your `<path-to-plot-data>/raw-data` directory

3) edit `RAW-DATA` and `TIMING` links in `calculator_stat_cpu.sh` or `calculator_stat_gpu.sh` and run the scripts to get the final plotting data in `TIMING` directory
